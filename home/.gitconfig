[init]
    defaultBranch = main
[core]
    editor = vim
[pull]
    rebase = false
[push]
    default = current

# Allow untracked local identity without committing to repo
[includeif "gitdir:~/"]
    path = ~/.gitconfig.local

